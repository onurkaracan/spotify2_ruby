<%= form_with(model: playlist) do |form| %>
  <% if playlist.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(playlist.errors.count, "error") %> prohibited this playlist from being saved:</h2>

      <ul>
        <% playlist.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :duration, style: "display: block" %>
    <%= form.number_field :duration %>
  </div>

  <div>
    <%= form.label :genre, style: "display: block" %>
    <%= form.text_area :genre %>
  </div>

  <div>
    <%= form.label :media_url, style: "display: block" %>
    <%= form.text_field :media_url %>
  </div>

  <%= form.collection_check_boxes(:song_ids, Song.order(:id), :id, :name) %>

  <div>
    <%= form.submit %>
  </div>
<% end %>

